<h1>Here are all the current exhibits on display at the Gallery!</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Category</th>
    <th>Period</th>
  </tr>
  <% for exhibit in @exhibits %>
  <tr>
    <td><a href="exhibits/<%= exhibit.id %>"><%= exhibit.name %></a></td>
    <td><%= exhibit.category %></td>
    <td><%= exhibit.period %></td>
  </tr>
  <% end %>
</table>
